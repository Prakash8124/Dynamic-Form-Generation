import { SelectionTextDirective } from './selection-text.directive';

describe('SelectionTextDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectionTextDirective();
    expect(directive).toBeTruthy();
  });
});
